#pragma once
// Repository-only include adapter for the existing, unmodified vendored JSON library.
// Public SDK headers use the canonical <nlohmann/json.hpp> dependency path.
#include "../json.hpp"
